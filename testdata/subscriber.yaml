version: 1

model:
  name: subscriber

  fields:
    - name: msisdn
      type: string
      required: true

    - name: status
      type: string
      required: true

    - name: category
      type: string

    - name: last_tx
      type: datetime
